<%-include('../header.ejs') %>
<main id="login-main">
    <section></section>
    <section id="login-section">
        <article>
            <div id="login-frm">
                <h4 class="nanum">로그인</h4>
                <form action="">
                    <input type="text" placeholder ="아이디" class="login-text">
                    <input type="text" placeholder="비밀번호" class="login-text">
                    <input type="submit" value="로그인" id="login-btn">
                </form>
            </div>
            <div id="auth-btn">
                <a href="">회원가입</a>
                <a href="">비밀번호 찾기</a>
            </div>
        </article>
        <article>
            <h4>소셜로그인</h4>
            <p>SNS계정을 통해 로그인해주세요</p>
            <button>Facebook</button>
            <script src="https://t1.kakaocdn.net/kakao_js_sdk/2.0.0/kakao.min.js"
  integrity="sha384-PFHeU/4gvSH8kpvhrigAPfZGBDPs372JceJq3jAXce11bVA6rMvGWzvP4fMQuBGL" crossorigin="anonymous"></script>
<script>
  Kakao.init('457bc0baab39156996248d5b7386f600'); // 사용하려는 앱의 JavaScript 키 입력
</script>

<a id="kakao-login-btn" href="javascript:loginWithKakao()">
  <img src="https://k.kakaocdn.net/14/dn/btroDszwNrM/I6efHub1SN5KCJqLm1Ovx1/o.jpg" width="222"
    alt="카카오 로그인 버튼" />
</a>
<p id="token-result"></p>

<script>
  function loginWithKakao() {
    Kakao.Auth.authorize({
      redirectUri: 'http://localhost:5001',
    });
  }

  // 아래는 데모를 위한 UI 코드입니다.
  displayToken()
  function displayToken() {
    var token = getCookie('authorize-access-token');

    if(token) {
      Kakao.Auth.setAccessToken(token);
      Kakao.Auth.getStatusInfo()
        .then(function(res) {
          if (res.status === 'connected') {
            document.getElementById('token-result').innerText
              = 'login success, token: ' + Kakao.Auth.getAccessToken();
          }
        })
        .catch(function(err) {
          Kakao.Auth.setAccessToken(null);
        });
    }
  }

  function getCookie(name) {
    var parts = document.cookie.split(name + '=');
    if (parts.length === 2) { return parts[1].split(';')[0]; }
  }
</script>
            <button>Naver</button>
        </article>
    </section>
</main>
<link rel="stylesheet" href="/css/login.css">
<%-include('../footer.ejs') %>